name: Extreme Proxy Update

on:
  schedule:
    - cron: '27 */3 * * *'
  push:
    paths:
      - 'my_personal_links.txt'
  workflow_dispatch:

jobs:
  update-proxies:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests maxminddb

      - name: Execute Worker
        run: python worker.py

      - name: Commit and Force Push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "ExtremeProxyBot"
          git config --global user.email "bot@github.com"
          git add my_stable_configs.txt my_personal_links.txt activity_log.txt
          git commit -m "Auto-Pulse & Trigger: $(date +'%Y-%m-%d %H:%M')" --allow-empty
          git push origin main
